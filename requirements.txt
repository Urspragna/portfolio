# Core API
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0

# Database (async)
sqlalchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0          # for PostgreSQL in production

# HTTP
httpx==0.28.1
python-multipart==0.0.20

# AI / RAG
numpy==1.26.4
sentence-transformers==3.3.1   # remove if you want a lighter image; falls back to hash embedder
transformers==4.47.1            # for the sentiment demo (optional — has lexicon fallback)
torch==2.5.1                    # transformers dependency; cpu-only is fine

# Optional: pgvector adapter (uncomment when moving to Postgres)
# pgvector==0.3.6
