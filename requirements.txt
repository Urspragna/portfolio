# Core API
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0

# Database (async)
sqlalchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0          # for PostgreSQL in production

# HTTP
httpx==0.28.1
python-multipart==0.0.20

# Security
slowapi==0.1.9    # per-IP rate limiting

# AI / RAG
numpy==1.26.4
# sentence-transformers, transformers, torch removed — too heavy for free hosting
# embedding falls back to hash-based; sentiment falls back to lexicon classifier

# Optional: pgvector adapter (uncomment when moving to Postgres)
# pgvector==0.3.6
